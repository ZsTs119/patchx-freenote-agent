# PatchXNote Agent 0.2.13 地址文案发布证据

日期：2026-09-11

## 范围

- 用户已授权提交与发布；仅限 Agent 仓库。GoServer 和飞书指南由用户另行维护。
- 基线：`4eae4e514307d1abc3db14f67687faa1349daa71`；已发布版本：`0.2.13`；发布提交：`b645657fbb9bd04bd5aeae510e5f6bf2895b955c`。
- 统一中英文一句话、README/npm/Skill/marketplace 副本、API/Remote MCP/接入/下载链接及 homepage 元数据。
- API 默认地址与 Go/安装器业务逻辑未变化，延续 `0.2.12`。
- 保留历史验收和路径前缀测试；未提交用户原有未跟踪文件。
- 仅主代理串行执行；审阅属于自审，不是独立审查。

## 本地验证

- PASS：`GOMAXPROCS=1 GOFLAGS=-p=1 go test -p 1 -parallel 1 ./...`，未变化的 Go 包复用测试缓存。
- PASS：`GOMAXPROCS=1 GOFLAGS=-p=1 scripts/e2e/mvp-smoke.sh`，以 `0.2.13` 构建、打包并执行协议/安装 smoke。
- PASS：npm 安装器测试、18 个客户端 registry、Skill 同步、Skill 与插件验证器。
- PASS：8 文件 13 段中英文一句话一致、图片链接、npm `0.2.13` pack dry-run（包含 Skill）。
- PASS：主代理自审和 `git diff --check`；未改 Go 业务代码或安装器逻辑。

## 发布

- PASS：[GitHub Release 34579786530](https://github.com/ZsTs119/patchxnote-agent/actions/runs/34579786530)，六个平台/架构二进制、SHA-256 清单和 artifact attestation 已发布。
- Release：[v0.2.13](https://github.com/ZsTs119/patchxnote-agent/releases/tag/v0.2.13)，tag 指向发布提交。
- PASS：[npm Trusted Publishing 34580083667](https://github.com/ZsTs119/patchxnote-agent/actions/runs/34580083667)，官方 registry version/latest 均为 `0.2.13`，homepage 为生产 `/mcp/setup/`，含 SLSA provenance。
- PASS：Windows 从实际 npm registry 包安装二进制，version/commit 与 tag 一致；`mcp config`、Cursor setup dry-run 通过。
- PASS：从 npm registry 下载的实际 tarball（9 文件）中 README、Skill onboarding、source-of-truth 与仓库源文件逐字一致。
- PASS：实际 npm 包 Skill 安装到独立临时目录，onboarding 与 canonical source 一致；source hash 为 `sha256:c7e5ace88fb4efc76c7e56df763e2c05f2b62524e3dae8c8389f65fdcca0d19e`。
- PASS：实际 npm → stdio → 生产 `/mcp` 顺序 initialize、tools/list（19 tools）、current-user、mobile memories（5 条），全部无错误；`mcp status --verify` 为 authenticated/verified。
- PASS：Linux amd64 实际执行 version/commit 校验；Linux 与 Windows 实际发布文件 checksum 和 `gh attestation verify` 通过。
- PASS：[macOS install/keychain/MCP 34580411989](https://github.com/ZsTs119/patchxnote-agent/actions/runs/34580411989)，原生钥匙串、实际 npm 安装与 MCP initialize/tools/list 通过。

| 制品 | SHA-256 |
| --- | --- |
| Linux amd64 | `f5542368d64b3bfe03e26b91d1ccb2631c816f4808a44017982c8016e7984856` |
| Windows amd64 | `c9e9c624f76d51ab82a3f1df27b9ff0f55e6e5b411b6a6b504b6f5bb246c82a9` |

本次 Agent 提交/发布范围内没有剩余阻塞。

## 证据边界

本次不重新发起生产浏览器授权；发布包复用了本会话已完成的生产 OAuth 原生凭据并验证了 stdio MCP 读工具。各编辑器 UI、云平台控制台与 GoServer 部署不由本次结果推导。
