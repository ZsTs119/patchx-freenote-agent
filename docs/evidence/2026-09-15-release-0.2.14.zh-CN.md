# 0.2.14 Registry 元数据修正与发布验证

日期：2026-09-15。状态：GitHub／npm 0.2.14 发布、Windows 安装与 MCP 协议验证、官方 Registry 登记及公开检索回读通过；浏览器截图仍待工具恢复。

## 范围与原因

- 用户已明确授权本次发行元数据修正、必要 Git 提交／推送、GitHub／npm 修正版发布及 MCP Registry 登记。
- 基线为公开 main `bf536290f06e9d576612a10c9d2d0e38627550cb`，相对 `v0.2.13` 只有文档／验收记录变化。
- Registry 实际 JWT 个人权限为 `io.github.ZsTs119/*`；旧 npm 与登记候选的 `io.github.zsts119/patchxnote-agent` 不匹配。令牌未写入证据。
- 修正 npm `mcpName` 与 Registry `name` 的大小写；版本同步为 `0.2.14`，并补齐短描述、`mcp serve` 参数、展示名及官网入口。
- Go 运行时、npm launcher 实现与 Skill 内容／许可不变。仅更新相关发行校验、工作流选测与文档。

## 相关验证

- 新增元数据校验在旧材料上复现大小写失败；修正后通过。官方 publisher validate、六平台安装计划、npm pack 九文件、四组 Git fixture 选测回归、工作流 YAML 和 Git whitespace 全部通过。
- 测试范围：发行字段和版本关联、六平台安装计划、npm pack、Windows 新版实际安装、initialize／tools/list、Registry publish 与回读。
- CI 按上个版本与发布提交的模块差异选择测试；纯元数据变更不触发全仓 Go 或完整安装器回归。缺少基线时保留原有测试。
- skills.sh 已可搜到 `zsts119/patchxnote-agent/patchxnote-mcp`，有效详情页为 https://skills.sh/ZsTs119/patchxnote-agent/patchxnote-mcp 。GitHub Skill 安装不因 npm 元数据升版重复执行。
- Chrome 工具 request-header policy 加载错误，浏览器截图暂未完成；HTTP/API 证据与浏览器验收分开记录。

## 正式发布结果

- 发行提交：`2dcebd0e6f67bcf4c856004a6058e4d0c50d4fe5`；已推送 main 和修正分支，`v0.2.14` 指向同一提交。原 workbuddy 工作区的功能文件未改动。
- [GitHub Release](https://github.com/ZsTs119/patchxnote-agent/releases/tag/v0.2.14) 已公开。[Release workflow](https://github.com/ZsTs119/patchxnote-agent/actions/runs/34925935935) 成功，六平台二进制与 checksums.txt 齐备，六个二进制实取 SHA-256 均匹配。`gh attestation verify` 对 checksums.txt 验证通过，并约束仓库、release.yml、tag 与上述源码提交。
- [npm 0.2.14](https://www.npmjs.com/package/patchxnote-agent/v/0.2.14) 已公开，latest=0.2.14，mcpName 大小写正确。[npm workflow](https://github.com/ZsTs119/patchxnote-agent/actions/runs/34926192210) 成功，[provenance](https://search.sigstore.dev/?logIndex=2839045430) 已发布；实取 tarball 的 SHA-512 与 registry integrity 一致。
- Windows 使用 npm 官方源执行真实安装通过：`patchxnote 0.2.14`、commit 与 tag 一致。实际 npm tarball 启动 `mcp serve`，initialize 与 tools/list 通过：协议 2025-06-18、19 工具、stderr 0 bytes；没有调用账号、记忆或模型工具。安装仅覆盖任务目录，未改 PATH 或默认 npm registry。
- [MCP Registry 搜索](https://registry.modelcontextprotocol.io/?q=patchxnote) 的服务端查询已命中准确条目；[0.2.14 官方版本记录](https://registry.modelcontextprotocol.io/v0.1/servers/io.github.ZsTs119%2Fpatchxnote-agent/versions/0.2.14) 可读，status=active、isLatest=true，名称、版本、npm 包、transport、启动参数和展示字段全部与候选一致。

本轮不包含服务端部署、账号／记忆／模型业务回归或新增客户端 UI 验收。

## 执行中的暂态情况

- npm 发布成功后经历平台处理窗口，初次公开查询未更新；后续 registry 回读和安装通过，没有重复发布。
- 本机默认 registry.npmmirror.com 初次安装返回 ETARGET；仅对本次命令指定 registry.npmjs.org 后成功，没有修改用户默认配置。
- Registry publish 已返回 active，但紧接的同一搜索回读未命中。随后独立版本详情与 patchxnote 搜索均通过，未重复 publish。
- Chrome 工具最后复查仍为 request-header policy 加载失败，因此未声称完成浏览器页面操作或截图验收。

## 可复制的 PPT 表述

> PatchXNote Agent 已通过 npm 与 GitHub Release 发布 0.2.14，并登记于 MCP 官方 Registry；配套 Skill 已被 Vercel skills.sh 收录，可通过公开渠道搜索与安装。本次已验证发行物校验和、Windows 安装和 MCP 协议发现。

本次由主代理自审，无新增业务逻辑变更；测试只涉及本轮发行、材料、安装、协议和目录模块。腾讯 SkillHub 本轮预检已完成，尚未上传；ClawHub 按原许可决策暂缓。