# PatchXNote Agent 0.2.12 生产地址切换与发布证据

日期：2026-09-11

## 范围与状态

- 用户已授权修改、验证、GitHub Release 和 npm 发布。
- 已发布版本：`0.2.12`；Release tag：`v0.2.12`。
- 发布提交：`18aa4c4e1f54a1a59036a749263410b34111c97d`；起始提交：`f8a2c1632fa056c5e147cfd6d8458603bc23c950`。
- 默认 API：`https://freenote.patch-x.cn`；Remote MCP：`https://freenote.patch-x.cn/mcp`。
- 改动限于默认地址、CLI 提示、分发版本和接入/迁移说明；发布测试与构建采用串行模式。
- 自审由主代理完成，不属于独立审查。

## 生产 HTTP 前置检查

- `/healthz`：200，服务正常。
- `/readyz`：200，PostgreSQL / Redis ready。
- OAuth discovery：200；issuer、authorize/token/revoke endpoint 与生产根地址一致，支持 PKCE S256。
- 现有 `patchxnote-local-dev` 的授权页：200，包含手机号登录表单。
- `/mcp` initialize：200；tools/list：200，19 tools。
- 未授权 current-user 工具调用：401，携带生产 protected-resource metadata 地址。
- 上述为 HTTP/协议检查，不能代替真实浏览器和授权后业务验收。

## 本地候选验证

- PASS：`go test -p 1 -parallel 1 ./...`，所有包通过。
- PASS：`GOMAXPROCS=1 GOFLAGS=-p=1 scripts/e2e/mvp-smoke.sh`；证据 `.tmp/e2e/evidence.json`。
- PASS：npm wrapper installer tests、18 个客户端 registry 校验。
- PASS：Skill 同步校验、Skill/插件验证器和 skill install dry-run。
- PASS：完整 npm package dry-run，包含 Skill；Windows amd64 候选构建及 `--from-local` 安装。
- PASS：候选 `mcp status` 在新 profile 中报告生产默认地址且 `no_credential`。
- 主代理自审：代码仅改默认值与帮助文案；既有配置优先级和 OAuth 地址绑定保持不变。

## 生产浏览器与授权后验收

- PASS：Windows 候选 `mcp login`，用户在指定 Chrome 扩展打开的生产授权页完成登录，OS-native keychain 保存成功。
- PASS：登录结果 `logged_in=true`、`smoke_verified=true`，生产 current-user 验证成功。
- 候选使用 `--no-browser` 后通过指定 Chrome 扩展打开页面，因此不计为默认浏览器自动打开验收。
- 根地址 `/` HTTP 为 404，Chrome 初次打开显示 `ERR_BLOCKED_BY_CLIENT`；实际 OAuth 授权端点正常。
- PASS：Chrome 页面确认授权成功；`mcp status --verify` 返回 authenticated/verified。
- PASS：候选 stdio initialize、tools/list（19 tools）、current-user 和 mobile memories（limit=5，返回 5 条），仅记录条数与通过状态。

## 发布与发布包验证

- PASS：[GitHub Release workflow 34577306079](https://github.com/ZsTs119/patchxnote-agent/actions/runs/34577306079)，全量测试、六个平台/架构二进制、SHA-256 清单和制品 attestation 均成功。
- 已发布：[v0.2.12](https://github.com/ZsTs119/patchxnote-agent/releases/tag/v0.2.12)，非 draft；release tag 指向上述发布提交。
- PASS：[npm Trusted Publishing workflow 34577650659](https://github.com/ZsTs119/patchxnote-agent/actions/runs/34577650659)，OIDC 发布成功；官方 registry 的 version 与 latest 均为 `0.2.12`，存在 SLSA provenance attestation。
- PASS：Windows 从官方 npm `patchxnote-agent@0.2.12` 安装真实 GitHub 发布文件，版本/提交与 tag 一致。
- PASS：Windows 实际安装二进制和实际 npm `mcp serve` 启动链路均完成顺序 initialize → tools/list（19）→ current-user → mobile memories（5 条），所有响应无错误。
- PASS：发布包 `mcp config` 纯 JSON、Cursor setup dry-run、独立临时目录 Skill 安装；Skill source hash 与源码候选一致。
- PASS：发布二进制 `mcp status --verify`，默认生产地址、authenticated/verified 为 true。发布包复用本轮已完成的生产 OAuth 原生安全存储凭据，没有重复执行 fresh-profile 浏览器登录。
- PASS：Linux amd64 发布文件 SHA-256、实际执行 version/commit 校验。
- PASS：Linux 和 Windows 实际发布文件的 `gh attestation verify --repo ZsTs119/patchxnote-agent` 均退出 0。
- PASS：[macOS install/keychain/MCP workflow 34578025372](https://github.com/ZsTs119/patchxnote-agent/actions/runs/34578025372)，原生钥匙串、npm 实际安装、版本/平台断言和 MCP initialize/tools/list 均通过。

| 已验证制品 | SHA-256 |
| --- | --- |
| Linux amd64 | `6ea8043b702d004956ddd8feafc8792fe0a5f28bfe3bfad83604dd478524fd44` |
| Windows amd64 | `2a4cfbfb9a32226e55c5dc2f7e313726dc5782c35f87a5a760e1fcfe8e478670` |

本次验收范围内没有遗留发布阻塞。各编辑器 UI、第三方云平台控制台和全产品生产验收不在本次证据范围内。

## 迁移边界

显式服务地址覆盖项继续优先生效；切换生产需要重新登录。此版本不迁移测试账号、记录或平台授权，不证明各编辑器/云平台控制台验收。

## 下游文档交接

GoServer 的飞书指南源文件仍记录 `0.2.1` 历史测试版本，当前 GoServer checkout 另有生产页面改动。本次不改该仓库或在线飞书页面；最新默认地址与迁移方式以本仓库中英文 README、npm README 和 Skill 为准。后续更新飞书指南时需同步生产 API/MCP 地址与 `0.2.12`，不能将旧指南的测试环境说明继续作为新版默认值。
